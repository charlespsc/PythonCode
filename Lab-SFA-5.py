# LAB   Operadores e Expressões
#
# Meu cálculo:
x = float(input("Digite o valor para x: "))
y = 1 / (x + (1 / (x + (1 / (x + (1 / x))))))
print("y =", y)
#
# Exemplos do Sistema:
x = float(input("Digite o valor para x: "))
y = 1./(x + 1./(x + 1./(x + 1./x)))
print("y =", y)